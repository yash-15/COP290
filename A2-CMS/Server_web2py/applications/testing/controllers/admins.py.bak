# -*- coding: utf-8 -*-
# try something like
def index(): 
    return list()

def list():
    if auth.is_logged_in():
        posts=db(db.administrators.cor_user==auth.user.id).select()
        return dict(z=auth.user.id)
    else:
        return dict()
